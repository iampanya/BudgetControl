<!DOCTYPE html>
<html ng-app="budgetApp" ng-cloak>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - ระบบควบคมงบประมาณ</title>
    @Styles.Render("~/Content/css")
    @*@Scripts.Render("~/bundles/modernizr")*@
    @Scripts.Render("~/bundles/polyfill")
    @Scripts.Render("~/bundles/angular")
    @Scripts.Render("~/bundles/angular-script")
    <base href="@Url.Content("~/")" />
</head>
<body>
    @RenderBody()
    @*<nav class="navbar navbar-default navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a href="#/budget" class="navbar-brand">ระบบควบคุมงบประมาณ</a>
            </div>


            <div ng-controller="userController">
                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
                        <li ui-sref-active="active"><a ui-sref="home">Home</a></li>
                        <li ui-sref-active="active"><a ui-sref="budget">Budget</a></li>
                        <li ui-sref-active="active"><a ui-sref="payment">Payment</a></li>
                    </ul>

                    <div ng-if="!isAuthen">
                        <ul class="nav navbar-nav navbar-right">
                            <li ui-sref-active="active"><a ui-sref="signin">เข้าสู่ระบบ</a></li>
                        </ul>
                    </div>

                    <div ng-if="isAuthen">
                        <ul class="nav navbar-nav navbar-right">
                            <li class="dropdown">
                                <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                                    {{ Authen.User.Employee.FirstName + ' ' + Authen.User.Employee.LastName }}
                                    <span class="caret"></span>
                                </a>
                                <ul class="dropdown-menu" role="menu">
                                    <li><a href="#">แก้ไขข้อมูลส่วนตัว</a></li>
                                    <li><a ui-sref="changepassword">เปลี่ยนรหัสผ่าน</a></li>
                                    <li class="divider"></li>
                                    <li><a ng-click="Logout()">ออกจากระบบ</a></li>
                                </ul>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>*@

    @*<div class="container-fluid">
        <div class="container body-content">
            @RenderBody()
        </div>

        <div class="navbar-fixed-bottom" style="background-color: #f7f7f7;">
            <hr class="footer" />
            <div class="container">
                <footer>
                    <p>&copy; @DateTime.Now.Year - ระบบควบคุมงบประมาณ</p>
                </footer>
            </div>
        </div>
    </div>*@

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
