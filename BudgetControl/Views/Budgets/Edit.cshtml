
<div>
    <div class="row">
        <div class="col-lg-6 col-lg-offset-3 col-md-6 col-md-offset-3">
            <div class="card">
                <form class="form-horizontal" name="frmEditBudget" ng-submit="vm.submit()" method="post" novalidate>
                    <fieldset>
                        <legend align="center" class="">แก้ไขข้อมูลงบประมาณ</legend>
                        <div class="form-group">
                            <div class="col-md-6 col-xs-6">
                                <h4 class="text-right">ปีงบประมาณ: </h4>
                            </div>
                            <div class="col-md-6 col-xs-6">
                                <h4 class="text-left text-primary">{{ vm.budget.Year }}</h4>
                            </div>
                        </div>

                        <div class="form-group">
                            <div class="col-md-6 col-xs-6">
                                <h4 class="text-right">รหัสบัญชี: </h4>
                            </div>
                            <div class="col-md-6 col-xs-6">
                                <h4 class="text-left text-primary">{{ vm.budget.AccountID }}</h4>
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-6 col-xs-6">
                                <h4 class="text-right">ชื่อบัญชี: </h4>
                            </div>
                            <div class="col-md-6 col-xs-6">
                                <h4 class="text-left text-primary">{{ vm.budget.Account.AccountName }}</h4>
                            </div>
                        </div>

                        <div class="form-group">
                            <div class="col-md-6 col-xs-6">
                                <h4 class="text-right">จำนวนเงิน: </h4>
                            </div>
                            <div class="col-md-4 col-xs-6">
                                <input type="number" class="form-control" id="txtBudgetAmount" name="budgetAmount" ng-required="true" ng-min=1 ng-model="vm.budget.BudgetAmount"/>

                                <span class="text-danger" ng-show="frmEditBudget.budgetAmount.$error.min">จำนวนเงินจะต้องมีค่ามากกว่า 0</span>
                                <span class="text-danger" ng-show="frmEditBudget.budgetAmount.$error.required">กรุณาระบุจำนวนเงิน</span>
                            </div>
                        </div>

                        <div class="form-group">
                            <div class="col-md-4 col-md-offset-2">
                                <a ui-sref="budget" class="btn btn-default btn-block">ยกเลิก</a>
                            </div>
                            <div class="col-md-4">
                                <button type="submit" class="btn btn-primary btn-block" ng-disabled="frmEditBudget.$invalid">
                                    <i class="fa fa-save" aria-hidden="true"></i> บันทึก
                                </button>
                            </div>
                        </div>
                    </fieldset>
                </form>
            </div>
        </div>
    </div>

</div>